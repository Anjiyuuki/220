

import graphics for library of classes
import math for library of math

point class that is in the graphic library
you create object using constructor
method is a function associated with the class
.move will move the point

Graphwin object
-title
-width
-height

variables are pointers to memories
"variable = variable" will point to the same memories ... you will have to clone
Ints, Floats, and String, New memories are created

Events :
- Mouse CLicks
- Key Strokes

user_input = Entry(Point(5,5)50)
user_input.setText("Enter your name here")
user_input.draw(win)
win.getMouse()
name = user_input.getText()

tes